# 本機端啟動指南

## 📋 前置準備

### 1. 從 GCloud Secret Manager 取得 API Keys

執行以下命令查看您的 Secrets：

```bash
# 查看所有 Secrets
gcloud secrets list

# 取得各個 Secret 的值（請根據實際名稱調整）
gcloud secrets versions access latest --secret="GOOGLE_CLIENT_ID"
gcloud secrets versions access latest --secret="GOOGLE_CLIENT_SECRET"
gcloud secrets versions access latest --secret="GEMINI_API_KEY"
gcloud secrets versions access latest --secret="GOOGLE_API_KEY"
gcloud secrets versions access latest --secret="SEARCH_ENGINE_ID"
```

### 2. 設定前端環境變數

編輯 `frontend/.env.local` 檔案，填入以下資訊：

```bash
# 1. 打開檔案
code frontend/.env.local

# 2. 填入從 GCloud 取得的值：
# - GOOGLE_CLIENT_ID=從上面命令取得的值
# - GOOGLE_CLIENT_SECRET=從上面命令取得的值
# - NEXTAUTH_SECRET=隨機產生一個 32 字元的字串

# 3. 產生 NEXTAUTH_SECRET（可用此命令）
openssl rand -base64 32
```

### 3. 設定後端環境變數

編輯 `.env` 檔案，填入以下資訊：

```bash
# 打開檔案
code .env

# 填入從 GCloud 取得的值：
# - GEMINI_API_KEY=
# - GOOGLE_API_KEY=
# - SEARCH_ENGINE_ID=
# - GOOGLE_CLIENT_ID=
```

---

## 🚀 啟動步驟

### 終端 1：啟動後端

```bash
# 1. 進入專案根目錄
cd /Users/stephen/Desktop/OderWhat

# 2. 啟動虛擬環境
source venv/bin/activate

# 3. 啟動後端伺服器
python main.py
```

**預期輸出**：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**測試後端**：
```bash
# 在另一個終端測試
curl http://localhost:8000/health
# 應該返回: {"status":"ok"}
```

---

### 終端 2：啟動前端

```bash
# 1. 進入前端目錄
cd /Users/stephen/Desktop/OderWhat/frontend

# 2. 安裝依賴（第一次需要）
npm install

# 3. 啟動開發伺服器
npm run dev
```

**預期輸出**：
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Ready in xxxms
```

---

## 🌐 開啟瀏覽器

訪問以下網址：
- **前端**: http://localhost:3000
- **後端 API**: http://localhost:8000/health

---

## ✅ 測試檢查清單

### 第一步：檢查首頁
- [ ] 訪問 `http://localhost:3000/`
- [ ] 應該看到「今天想吃哪一家？」的輸入頁面
- [ ] **不應該**看到 "Don't think. Just eat." 的 Landing Page
- [ ] 右上角有「Google 登入」按鈕

### 第二步：測試登入流程
- [ ] 餐廳名稱 Input 應該是停用狀態（灰色）
- [ ] 顯示橘色提示卡片「請先登入以使用 AI 點餐」
- [ ] 點擊「使用 Google 登入」按鈕
- [ ] 跳轉到 Google OAuth 頁面
- [ ] 選擇帳號並授權
- [ ] 返回網站後，Input 自動啟用
- [ ] 右上角顯示使用者頭像和名稱

### 第三步：測試推薦功能
- [ ] 登入後，輸入餐廳名稱（例如：「鼎泰豐」）
- [ ] 點擊「下一步」
- [ ] 填寫人數、預算、飲食偏好
- [ ] 點擊「開始生成推薦」
- [ ] 顯示載入動畫「正在爬梳 580 則 Google 評論...」
- [ ] 等待 3-5 秒後顯示推薦結果
- [ ] **檢查重點**：推薦結果**不應該**包含「模擬」、「測試」等字樣
- [ ] 菜名、價格、評論應該是真實資料

### 第四步：測試錯誤處理
如果想測試錯誤訊息（可選）：

```bash
# 暫時停止後端（在後端終端按 Ctrl+C）
# 然後在前端嘗試提交推薦
# 應該顯示：「網路連線異常，請檢查網路後重試」
```

---

## 🐛 常見問題排解

### 問題 1：前端無法啟動
**錯誤**：`Error: Cannot find module 'next'`
**解決**：
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### 問題 2：登入後跳轉回來但未登入
**原因**：`NEXTAUTH_SECRET` 或 `GOOGLE_CLIENT_ID` 設定錯誤
**解決**：
1. 檢查 `frontend/.env.local` 中的值是否正確
2. 確認 Google OAuth 的 Redirect URI 包含 `http://localhost:3000/api/auth/callback/google`

### 問題 3：推薦結果顯示「服務暫時無法使用」
**原因**：後端 `GEMINI_API_KEY` 未設定或無效
**解決**：
1. 檢查 `.env` 中的 `GEMINI_API_KEY` 是否正確
2. 查看後端終端的錯誤訊息
3. 確認 API Key 在 Google AI Studio 中有效且有剩餘額度

### 問題 4：顯示「模擬資料」
**原因**：使用了舊的程式碼版本
**解決**：
```bash
# 確認後端程式碼已更新
grep -n "模擬資料" agent/dining_agent.py
# 應該找不到任何結果
```

---

## 📝 快速啟動腳本（可選）

建立一個快速啟動腳本：

```bash
# 建立 start.sh
cat > start.sh << 'EOF'
#!/bin/bash
echo "🚀 啟動 OderWhat 本機開發環境..."

# 啟動後端（背景執行）
echo "📡 啟動後端..."
cd /Users/stephen/Desktop/OderWhat
source venv/bin/activate
python main.py &
BACKEND_PID=$!

# 等待後端啟動
sleep 3

# 啟動前端
echo "🌐 啟動前端..."
cd frontend
npm run dev

# 當前端停止時，也停止後端
kill $BACKEND_PID
EOF

chmod +x start.sh
```

然後只需執行：
```bash
./start.sh
```

---

## 🎉 成功標誌

如果一切順利，您應該看到：
1. ✅ 首頁直接顯示輸入介面（無 Landing Page）
2. ✅ 未登入時 Input 停用且顯示登入提示
3. ✅ 登入後可正常輸入餐廳名稱
4. ✅ 推薦結果顯示真實資料（無「模擬」字樣）
5. ✅ 錯誤訊息友善且為繁體中文

---

**需要協助？** 請檢查：
- 後端終端的錯誤訊息
- 前端終端的錯誤訊息
- 瀏覽器 Console (F12) 的錯誤訊息
