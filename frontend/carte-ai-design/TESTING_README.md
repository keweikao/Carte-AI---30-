# Carte AI 設計遷移測試工具

本目錄包含用於驗證 Carte AI 設計系統遷移完整性的測試工具。

## 📋 測試工具清單

### 1. `test_migration_completeness.py`
完整性測試腳本,根據 `LLM_MIGRATION_PROMPT.md` 規格檢查所有實作項目。

**測試範圍**:
- ✅ 設計系統 (色彩、字體、陰影)
- ✅ 頁面結構 (5 個必要頁面)
- ✅ 元件系統 (7 個核心元件)
- ✅ UI 樣式規範 (按鈕、卡片、響應式)
- ✅ 功能實作 (導航、狀態管理)

**使用方法**:
```bash
python3 test_migration_completeness.py
```

**輸出**:
- 終端機顯示詳細測試結果
- 生成 `test_migration_report.json` (JSON 格式報告)

---

### 2. `show_test_summary.py`
視覺化測試摘要,以更友善的方式呈現測試結果。

**使用方法**:
```bash
python3 show_test_summary.py
```

**輸出範例**:
```
======================================================================
🎯 Carte AI 設計遷移測試摘要
======================================================================

完成度: 100.0%
[██████████████████████████████████████████████████] 57/57

📊 分類統計:

✅ 設計系統      12/12 (100.0%)
✅ 頁面結構      17/17 (100.0%)
✅ 元件系統       9/9  (100.0%)
✅ UI 樣式       11/11 (100.0%)
✅ 功能實作       8/8  (100.0%)

======================================================================
🎉 恭喜!所有測試項目都已通過!
======================================================================
```

---

### 3. `verify_migration.sh`
快速驗證腳本,一鍵執行完整測試流程。

**使用方法**:
```bash
./verify_migration.sh
```

**功能**:
- 執行完整性測試
- 顯示測試結果
- 提供報告檔案位置

---

## 📄 測試報告

### JSON 報告
**檔案**: `test_migration_report.json`

包含所有測試項目的詳細資訊:
```json
{
  "passed": 57,
  "failed": 0,
  "warnings": 1,
  "details": {
    "passed": [...],
    "failed": [...],
    "warnings": [...]
  }
}
```

### Markdown 報告
**檔案**: `docs/MIGRATION_TEST_REPORT.md`

人類可讀的完整測試報告,包含:
- 測試結果總覽
- 詳細測試項目
- 改進歷程
- 程式碼品質指標
- 最佳實踐遵循

---

## 🚀 快速開始

### 第一次使用

1. **執行完整測試**:
   ```bash
   python3 test_migration_completeness.py
   ```

2. **查看視覺化摘要**:
   ```bash
   python3 show_test_summary.py
   ```

3. **閱讀詳細報告**:
   ```bash
   cat docs/MIGRATION_TEST_REPORT.md
   ```

### 日常使用

使用快速驗證腳本:
```bash
./verify_migration.sh
```

---

## 📊 測試標準

所有測試基於 `docs/LLM_MIGRATION_PROMPT.md` 中定義的規格:

### 設計系統
- 色彩變數: `--charcoal`, `--caramel`, `--terracotta`, `--cream`, `--cream-dark`
- 字體變數: `--font-serif`, `--font-sans`
- 陰影變數: `--shadow-subtle`, `--shadow-medium`, `--shadow-floating`

### 頁面
- `/` - Landing Page
- `/input` - 輸入表單 (4 步驟)
- `/waiting` - 等待畫面 (3 階段)
- `/recommendation` - 推薦結果
- `/final-menu` - 最終菜單

### 元件
- `header.tsx` - 頂部導覽
- `footer.tsx` - 頁尾
- `progress-bar.tsx` - 進度指示器
- `dish-card.tsx` - 菜色卡片
- `menu-summary.tsx` - 菜色摘要
- `empty-state.tsx` - 空狀態
- `error-state.tsx` - 錯誤狀態

### UI 樣式
- Primary Button: 漸層 + 圓角
- Cards: `rounded-2xl` + shadow
- 響應式: `sm:`, `md:`, `lg:` 斷點

---

## 🔧 自訂測試

### 新增測試項目

編輯 `test_migration_completeness.py`:

```python
def test_my_feature():
    """檢查我的功能"""
    print("\n🔍 檢查我的功能...")
    
    # 你的測試邏輯
    if condition:
        result.add_pass("我的功能", "已實作")
    else:
        result.add_fail("我的功能", "未實作")

# 在 run_all_tests() 中加入
def run_all_tests():
    # ... 其他測試
    test_my_feature()
    # ...
```

### 調整測試標準

修改 `test_migration_completeness.py` 中的檢查邏輯:

```python
# 例如:調整必要的色彩變數
required_colors = {
    "--charcoal": "#2C2C2C",
    "--caramel": "#D4A574",
    # 加入新的色彩
    "--my-color": "#ABCDEF"
}
```

---

## 📈 測試結果解讀

### 完成度評級

- **100%**: 🎉 完美!所有項目通過
- **90-99%**: 👍 優秀,少數項目需修復
- **70-89%**: ⚠️ 良好,仍有改進空間
- **<70%**: ❌ 需要大量修復

### 測試狀態

- **✅ 通過**: 項目已正確實作
- **❌ 失敗**: 項目缺失或不符合規格
- **⚠️ 警告**: 項目可選或需要注意

---

## 🐛 常見問題

### Q: 測試失敗怎麼辦?

1. 查看詳細錯誤訊息
2. 對照 `LLM_MIGRATION_PROMPT.md` 規格
3. 修復缺失的項目
4. 重新執行測試

### Q: 如何查看特定測試項目?

查看 `test_migration_report.json`:
```bash
cat test_migration_report.json | jq '.details.failed'
```

### Q: 測試報告在哪裡?

- JSON 報告: `test_migration_report.json`
- Markdown 報告: `docs/MIGRATION_TEST_REPORT.md`

---

## 📚 相關文件

- `docs/LLM_MIGRATION_PROMPT.md` - 設計遷移規格
- `docs/MIGRATION_TEST_REPORT.md` - 完整測試報告
- `docs/CARTE_AI_DESIGN_HANDOFF.md` - 設計交接文件

---

## 🎯 目標

確保 Carte AI 設計系統遷移:
- ✅ 完整性 - 所有必要項目都已實作
- ✅ 一致性 - UI 樣式統一
- ✅ 規範性 - 符合設計規格
- ✅ 可維護性 - 程式碼結構清晰

---

**最後更新**: 2025-12-05  
**測試版本**: v1.0  
**完成度**: 100% (57/57)
